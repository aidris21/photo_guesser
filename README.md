# photo_guesser
A christmas present for my one and only. I hope this is a fun way to explore memory lane.

## Setup

```bash
npm install
npm run dev
```

Create a `.env` file with your Google Maps API key:

```bash
GOOGLE_MAPS_API_KEY=your_key_here
```
